spring:
  application:
    name: patient-config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/patrickschlaepfer/microservice-sample-config
server:
  port: 8888