spring:
  application:
    name: patient-config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/patrickschlaepfer/microservice-health-config.git
server:
  port: 8888